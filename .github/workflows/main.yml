name: ffmpeg-win-arm64 automated builds

on:
  workflow_dispatch:
    inputs:
      github_tag:
        description: 'Version Tag to build'
        required: true
        default: "n8.0.1"
        type: string
      build_type:
        description: 'Build type of executable'
        required: true
        default: 'essentials'
        type: choice
        options:
        - essentials
        - full
        - both
      link_type:
        description: 'Link type of libraries'
        required: true
        default: 'shared'
        type: choice
        options:
        - shared
        - static
      msystem:
        description: 'MSYS2 System Environment'
        required: true
        default: 'CLANGARM64'
        type: choice
        options:
        - CLANG64
        - CLANGARM64
      environment:
        description: 'Deploy Environment'
        required: true
        default: 'base'
        type: environment
  push:
    paths-ignore:
      - '**.md'
      - '.vscode/**'
      # - '.github/**'
      - '.devcontainer/**'
  pull_request:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  inputs_github_tag: "n8.0.1"
  inputs_build_type: "essentials"
  inputs_link_type: "shared"
  inputs_msystem: 'CLANGARM64'
  inputs_environment: 'base'
  msys_prefix: 'mingw-w64'
  toolchain_arch: 'clang-aarch64'
  package_prefix: 'mingw-w64-clang-aarch64'
  source_repo: "https://github.com/FFmpeg/FFmpeg"
  source_version: "0"

jobs:
  setup-defaults:
    name: '🌐 Setup Environment Variables'
    runs-on: ubuntu-latest
    environment: 'base'
    steps:
      - name: '🗺️ Map Workflow Inputs to Environment Variables'
        shell: bash
        run: |
          github_tag="${{ inputs.github_tag || 'n8.0.1' }}"
          echo "inputs_github_tag=${github_tag}" >> $GITHUB_ENV

          build_type="${{ inputs.build_type || 'essentials' }}"
          echo "inputs_build_type=${build_type}" >> $GITHUB_ENV

          link_type="${{ inputs.link_type || 'shared' }}"
          echo "inputs_link_type=${link_type} " >> $GITHUB_ENV

          msystem="${{ inputs.msystem || 'CLANGARM64' }}"
          echo "inputs_msystem=${msystem}" >> $GITHUB_ENV

          environment="${{ inputs.environment || 'CLANGARM64' }}"
          echo "inputs_environment=${environment}" >> $GITHUB_ENV
      - name: '🗺️ Map Toolchain Architecture Name'
        shell: bash
        run: |
          if [[ ${inputs_msystem} == "CLANGARM64" ]]; then
              echo "toolchain_arch=clang-aarch64" >> $GITHUB_ENV
              echo "package_prefix=${msys_prefix}-clang-aarch64" >> $GITHUB_ENV
          elif [[ ${inputs_msystem} == "CLANG64" ]]; then
              echo "toolchain_arch=clang-x86_64" >> $GITHUB_ENV
              echo "package_prefix=${msys_prefix}-clang-x86_64" >> $GITHUB_ENV
          fi
      - name: '🗺️ Verify Environment Variables'
        shell: bash
        run: |
          echo $inputs_environment
  clone-source:
    name: "🌀 Clone Source"
    runs-on: ubuntu-latest
    environment: 'base'
    steps:
      - name: "🌪️ Clone FFmpeg source repository"
        shell: bash
        run: |
          pwd
          # mkdir source
          # git clone --branch $inputs_github_tag $source_repo ./source
          # cd source
          # if test -f "RELEASE"; then
          #   version=$(<RELEASE)
          #   echo "source_version=$version" >> $GITHUB_ENV
          # else
          #   echo "release version not found"
          #   exit 1
          # fi
          ls -la
  download-external-packages:
    name: "⬇️ Download External Packages"
    runs-on: ubuntu-latest
    environment: 'base'
    steps:
      - name: '🛒 Checkout'
        uses: actions/checkout@v5
      - id: download-external-packages
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd $GITHUB_WORKSPACE
          # mkdir downloads
          # gh release download "${toolchain_arch}" -D "${GITHUB_WORKSPACE}/downloads" -R tordona/mingw-packages
          # cd downloads
          # ls -la
  download-external-utilities:
    name: "⬇️ Download External Helpers"
    runs-on: ubuntu-latest
    environment: 'base'
    steps:
      - name: '🛒 Checkout'
        uses: actions/checkout@v5
      - id: download-external-utilities
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # cd $GITHUB_WORKSPACE
          # mkdir utilities
          # gh release download "rcedit" -D "${GITHUB_WORKSPACE}/utilities" -R tordona/win-arm64-binaries
          # cd utilities
          ls -la
  msys2:
    name: '💻 MSYS2 Environment'
    needs: ['setup-defaults', 'clone-source', 'download-external-packages']
    runs-on: windows-11-arm
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    defaults:
      run:
        shell: msys2 {0}
    steps:
    - name: '🔧 Setup MSYS2'
      uses: msys2/setup-msys2@v2
      with:
        msystem: ${{ env.inputs_msystem }}
        update: true
        cache: true
        release: true
        install:
            git
            make
        pacboy:
            toolchain:p
            gcc-compat:p
            jq:p
            github-cli:p
    - name: '🛒 Checkout'
      uses: actions/checkout@v5
    - name: "📦 Install support packages - Generic Build"
      working-directory: ./config
      run: |
        echo "Install support packages - Generic Build"
        packages=$(jq -r --arg prefix "$package_prefix" '.options.base.extras | map(.package) | map($prefix + "-" + .) | join(" ")' options.json)
        pacman -S --noconfirm --overwrite '*' $packages
    - name: "📦 Install base level packages - Essentials Build"
      working-directory: ./config
      run: |
        echo "Install base packages - Essentials Build"
        packages=$(jq -r --arg prefix "$package_prefix" '.options.essentials.base | map(.package) | map($prefix + "-" + .) | join(" ")' options.json)
        pacman -S --noconfirm --overwrite '*' $packages
    - name: "📦 Install base level packages - Full Build"
      working-directory: ./config
      run: |
        echo "Install base packages - Full Release build"
        packages=$(jq -r --arg prefix "$package_prefix" '.options.full.base | map(.package) | map($prefix + "-" + .) | join(" ")' options.json)
        pacman -S --noconfirm --overwrite '*' $packages
    - name: "⬇️ Download External Packages"
      run: |
        mkdir packages
        gh release download "${toolchain_arch}" -D "./packages" -R tordona/mingw-packages
    - name: "➡️ Install External Packages"
      run: |
        echo "Install Custom Packages from External Repo"
        cd packages
        rm *.sig

        # preinstall(s) to protect against dependency cycle
        pacman -U --overwrite '*' --noconfirm *freetype*.pkg.tar.zst

        # batch install locally downloaded packages
        pacman -U --overwrite '*' --noconfirm *.pkg.tar.zst
    - name: "📦 Install external dependent packages - Essentials Build"
      working-directory: ./config
      run: |
        echo "Install external dependent packages - Essentials Release build"
        packages=$(jq -r --arg prefix "$package_prefix" '.options.essentials.dependent | map(.package) | map($prefix + "-" + .) | join(" ")' options.json)
        pacman -S --noconfirm --overwrite '*' $packages
    - name: "📦 Install external dependent packages - Full Build"
      working-directory: ./config
      run: |
        echo "Install external dependent packages - Full Release build"
        packages=$(jq -r --arg prefix "$package_prefix" '.options.full.dependent | map(.package) | map($prefix + "-" + .) | join(" ")' options.json)
        pacman -S --noconfirm --overwrite '*' $packages
    - name: "⬇️ Download External Utlities"
      run: |
        mkdir utilities
        gh release download "rcedit" -D "./utilities" -R tordona/win-arm64-binaries
        cd utilities
        ls -la
    - name: "🌪️ Clone FFmpeg source repository"
      run: |
        pwd
        mkdir source
        git clone --branch $inputs_github_tag $source_repo ./source
        cd source
        if test -f "RELEASE"; then
          version=$(<RELEASE)
          echo "source_version=$version" >> $GITHUB_ENV
        else
          echo "release version not found"
          exit 1
        fi
        ls -la
    - name: "🔨 Prebuild: Configure FFmpeg"
      run: |
        pwd
        ls -la
        cd source
        ls -la
        ./configure
  # # setup-toolchain-info:
  #   needs: ['msys2', 'clone-source']
  #   name: 'Setup Environment Toolchain'
  #   runs-on: windows-11-arm
  #   steps:
  #     - uses: actions/checkout@v5
  #     - uses: msys2/setup-msys2@v2
  #       with:
  #         msystem: ${{ env.inputs_msystem }}
  #         release: false
  #         run: |
  #           packages=$(jq -r '.options.base.extras | map(.package) | join(",")' options.json)
  #           echo $prefix-$toolchain_arch-{$packages}