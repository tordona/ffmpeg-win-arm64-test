name: ffmpeg-win-arm64 automated builds

on:
  workflow_dispatch:
    inputs:
      github_tag:
        description: 'Version Tag to build'
        required: true
        default: "n8.0.1"
        type: string
      build_type:
        description: 'Build type of executable'
        required: true
        default: 'essentials'
        type: choice
        options:
        - essentials
        - full
      link_type:
        description: 'Link type of libraries'
        required: true
        default: 'shared'
        type: choice
        options:
        - shared
        - static
      msystem:
        description: 'MSYS2 System Environment'
        required: true
        default: 'CLANGARM64'
        type: choice
        options:
        - CLANG64
        - CLANGARM64
  push:
    paths-ignore:
      - '**.md'
      - '.vscode/**'
      # - '.github/**'
      - '.devcontainer/**'
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  inputs_tag: "n8.0.1"
  inputs_build_type: "essentials"
  inputs_link_type: "shared"
  inputs_msystem: 'CLANGARM64'
  msys_prefix: 'mingw-w64'
  toolchain_arch: 'clang-aarch64'
  package_prefix: 'mingw-w64-clang-aarch64'

jobs:
  map-inputs-to-env-vars:
    name: '🛒 Map Workflow Inputs to Environment Variable'
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: '🛒 Map Inputs'
        run: |
          if [[ -n ${{ inputs.github_tag }} ]]; then
            echo "inputs_github_tag=${{ inputs.github_tag }}" >> $GITHUB_ENV
          fi
          if [[ -n ${{ inputs.build_type }} ]]; then
            echo "inputs_build_type=${{ inputs.build_type }}" >> $GITHUB_ENV
          fi
          if [[ -n ${{ inputs.link_type }} ]]; then
            echo "inputs_link_type=${{ inputs.link_type }}" >> $GITHUB_ENV
          fi
          if [[ -n ${{ inputs.msystem }} ]]; then
            echo "inputs_msystem=${{ inputs.msystem }}" >> $GITHUB_ENV
          fi
  msys2:
    name: '🧑‍💻 MSYS2 Environment'
    uses: ./.github/workflows/msys2.yml
    secrets: inherit
    with:
      environment: 'base'
      msystem: ${{ inputs.msystem }}
  setup-toolchain-info:
    needs: ['msys2']
    name: 'Setup Environment Toolchain Basics'
    runs-on: windows-11-arm
    steps:
      - uses: msys2/setup-msys2@v2
      - name: 'test'
        shell: msys2 {0}
        run: |
          echo ${${{ inputs.msystem }},,}
          echo $msys_prefix
          echo $toolchain_arch
          if [[ ${${{ inputs.msystem }},,} == "clangarm64" ]]; then
              echo "toolchain_arch=clang-aarch64" >> $GITHUB_ENV

          elif [[ ${${{ inputs.msystem }},,} == "clang64" ]]; then
              echo "toolchain_arch=clang-x86_64" >> $GITHUB_ENV
          fi
          echo "package_prefix=$msys_prefix-$toolchain_arch" >> $GITHUB_ENV
          echo $package_prefix